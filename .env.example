# 🎸 JamRoom Environment Variables Template
# Copy this file to .env and fill in your values
# Or run: node scripts/setup/createEnvFile.js

# 📊 Database Configuration
MONGODB_URI=mongodb://localhost:27017/jamroom
# Production: mongodb+srv://user:password@cluster.mongodb.net/jamroom?retryWrites=true&w=majority

# 🔐 Security Configuration (Generate a random 64+ character string)
JWT_SECRET=your-super-secret-jwt-key-minimum-64-characters-long

# 📧 Email Configuration (Gmail SMTP)
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_gmail_app_password

# 🌐 Server Configuration
PORT=5000
BASE_URL=http://localhost:5000
NODE_ENV=development

# 📝 Setup Notes:
# - For Gmail: Enable 2FA and create App Password at https://support.google.com/mail/answer/185833
# - For MongoDB Atlas: Whitelist IP addresses (0.0.0.0/0 for testing)  
# - JWT_SECRET: Use a random string with minimum 64 characters
# - Never commit .env to version control
# - Run 'node scripts/setup/createEnvFile.js' for interactive setup
